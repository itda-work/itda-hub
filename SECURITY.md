# 보안 신고

취약점은 공개 이슈로 올리지 말고 dev@itda.work 로 보내 주세요. 접수 후 3 영업일 안에 답합니다.

범위: 허브 서비스(`hub.itda.work`)와 이 저장소의 코드. 특히 OAuth 인가 서버, 도구 설정 값의 암호화 저장, 도구 호출 권한 검사, 궤적의 개인정보 노출.

허브는 사용자의 도구 설정 값(API 키 등)을 서버 측에 암호화해 보관합니다. 값은 화면·로그·궤적 어디에도 평문으로 남기지 않는 것이 계약이며, 이 계약을 어기는 경로를 발견하면 위 주소로 알려 주세요.
